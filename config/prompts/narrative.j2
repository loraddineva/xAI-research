An AI model was used to {{ task_description }}. The input features describe the instance, and the target variable indicates whether the instance belongs to the class "{{ positive_class_label }}".

An AI model estimated a {{ "%.0f"|format(pred_proba * 100) }}% probability that the instance belongs to the positive class, "{{ positive_class_label }}". Based on this probability, the model's final predicted class was "{{ pred_class_text }}". The SHAP table below explains how each feature contributed to this prediction. Positive SHAP values push toward "{{ positive_class_label }}"; negative SHAP values push toward "{{ negative_class_label }}". Contributions are sorted from most positive to most negative (this is not necessarily the same as importance rank by absolute value).

## Instance profile (feature values for this person)

{{ instance_snapshot }}

## SHAP contributions (model attributions only)

{{ shap_table }}

Can you come up with a plausible, fluent story as to why the model could have predicted this outcome? Use the instance profile to describe who this person is, and the SHAP table to explain what drove the score. Focus on features with the largest absolute SHAP contributions. For each feature you discuss, keep the direction of influence consistent with its SHAP sign. Do not treat feature values as if they were SHAP scores. Conclude with a summary of why this classification may have occurred.
